# polytess — runtime dependencies
PySide6>=6.6          # GUI (Qt for Python, LGPL)
qasync>=0.27          # asyncio <-> Qt event loop
cryptography>=42      # Ed25519 commercial-license-file verification
reportlab>=4          # flow documentation PDF export

# development / tests
pytest>=8
pytest-asyncio>=0.23

# your own plugins (e.g. under plugins/<name>) may need further
# dependencies — see that plugin's own pyproject.toml
