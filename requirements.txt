# polytess — Laufzeit-Abhängigkeiten
PySide6>=6.6          # GUI (Qt for Python, LGPL)
qasync>=0.27          # asyncio <-> Qt Event-Loop
cryptography>=42      # Ed25519 commercial-license-file verification
reportlab>=4          # flow documentation PDF export

# Entwicklung / Tests
pytest>=8
pytest-asyncio>=0.23

# eigene Plugins (z. B. in plugins/polytess_meshvary o.ä.) können weitere
# Abhängigkeiten benötigen — siehe deren jeweiliges pyproject.toml
